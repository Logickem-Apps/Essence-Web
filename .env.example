# Backend connectivity (dormant for now — set when a backend exists)
# PocketBase base URL (e.g. https://db.tudominio.com)
VITE_POCKETBASE_URL=
# API server base URL (e.g. https://api.tudominio.com)
VITE_API_URL=
